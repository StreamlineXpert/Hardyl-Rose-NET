# packages.yml

- name: Install packages
  apt:
    name:
      # List of your packages here
    state: latest

- name: Remove packages
  apt:
    autoremove: yes
    purge: yes
    name:
      # List of packages to remove
    state: absent
