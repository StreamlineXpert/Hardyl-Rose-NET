# tasks/users.yml

- name: create ansible user
  user:
    name: ansible
    system: yes

- name: copy sudoers_ansible
  copy:
    src: files/sudoers_ansible
    dest: /etc/sudoers.d/ansible
    owner: root
    group: root
    mode: 0440

- name: Create Standard user Hardel Rose
  user:
    name: "hardelrose"
    password: "2024"
    system: yes
    comment: "Hardel Rose"

